{application, example, [
  {description, "feeder http streaming example"},
  {vsn, "2.0.0"},
  {id, "git"},
  {modules, [
    example,
    example_sup,
    example_parse,
    example_logger
  ]},
  {registered, [example_sup, example_event_man]},
  {applications, [
    kernel,
    stdlib,
    inets,
    ssl
  ]},
  {mod, {example_app, []}},
  {env, []}
]}.
