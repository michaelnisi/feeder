{application, example, [
	{description, "XML feed stream parsing example"},
	{vsn, "1.0.0"},
	{id, "git"},
	{modules, []},
	{registered, [example_sup]},
	{applications, [
		kernel,
		stdlib,
    inets
	]},
	{mod, {example_app, []}},
	{env, []}
]}.
